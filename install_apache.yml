---
- hosts: all
  become: true
  tasks:
    - name: install apache
      ansible.builtin.package:
        name: apache2
        state: present
    - name: copy html
      ansible.builtin.copy:
        src: index.html
        dest: /var/www/html/
